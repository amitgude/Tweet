<div id="tweet_<%=micropost.id%>">
<h3> <%= micropost.tweet %> </h3>
  <div id="tweet-info">
     <p> <%= link_to "delete", micropost_path(micropost), method: :delete, remote: true %> </p>
     <p> <%= time_ago_in_words(micropost.created_at) %> ago </p>
  </div>
</div>
